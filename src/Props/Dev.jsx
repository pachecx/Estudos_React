import React from 'react'

export const Dev = ({name, age, job}) => {
  return (
    <div>
        <h2>Developer</h2>
        <p>Name: {name}</p>
        <p>Age: {age}</p>
        <p>Job: {job}</p>
    </div>
  )
}
